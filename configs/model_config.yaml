# 模型配置
cdr_algorithm:
  derivative_calculation: true
  distance_metric: "absolute_difference"
  warping_path: "dynamic_programming"

network_construction:
  method: "dfcn-swdrc"
  correlation_type: "pearson"
  reconstruction: true
  normalization: true

fdn:
  enabled: true
  delay_calculation: "mean_index_difference"
  directed: true